import "package:flutter/material.dart";
import "package:shoppingapp/model/datamodel.dart";

class BottomCards extends StatelessWidget {
  const BottomCards({super.key, required this.item});

  final Data item;

  @override
  Widget build(context) {
    return const Text("Sajal");
  }
}
